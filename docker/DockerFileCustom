FROM cu130:latest

# RUN pip install --break-system-packages --no-cache-dir --ignore-installed pip setuptools wheel && \
#     \
#     # Ставим PyTorch
#     pip install --break-system-packages --no-cache-dir torch==2.10.0 torchvision --index-url https://download.pytorch.org/whl/cu130 && \
#     \
#     # Фиксируем версии PyTorch и ВСЕХ компонентов TensorRT
#     CURRENT_TORCH_VERSION=$(python3 -c "import torch; print(torch.__version__)") && \
#     echo "torch==$CURRENT_TORCH_VERSION" > /tmp/constraints.txt && \
#     CURRENT_TRT_VERSION=$(python3 -c "import tensorrt; print(tensorrt.__version__)") && \
#     echo "tensorrt==$CURRENT_TRT_VERSION" >> /tmp/constraints.txt && \
#     echo "tensorrt_lean==$CURRENT_TRT_VERSION" >> /tmp/constraints.txt && \
#     echo "tensorrt_dispatch==$CURRENT_TRT_VERSION" >> /tmp/constraints.txt && \
#     \
#     # Ставим TensorRT-LLM из индекса NVIDIA, запрещая менять версии TRT и Torch
#     pip install --break-system-packages --no-cache-dir tensorrt_llm \
#         --extra-index-url https://pypi.nvidia.com \
#         -c /tmp/constraints.txt --verbose --verbose && \
#     \
#     # Удаляем временные файлы
#     rm -f /tmp/constraints.txt
    
WORKDIR /app
